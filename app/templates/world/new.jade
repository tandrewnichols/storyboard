.row
  .col-xs-12
    h4.text-center Create a new world. Only required fields are included here, but you can add a lot more detail later.
    hr
.row
  .col-xs-12
    form.form-horizontal(name="worldForm", novalidate, blur-track="true")
      .form-group(ng-class="{ 'has-error': !edit.name }", autofocus="")
        label.col-xs-2.control-label(for="name") Name
        .col-xs-10
          input#name.form-control(type="text", name="name", ng-model="edit.name")
          div.alert.alert-danger.mtm(ng-show="!edit.name && worldDetailsForm.name.$blurred") Name cannot be blank
      .col-xs-10.col-sm-offset-2(error="error", dismissed="dismissed", ng-show="error && !dismissed")
      .form-group
        .col-xs-10.col-sm-offset-2
          button.btn.btn-primary(type="submit", ng-click="update()", ng-disabled="!edit.name") Create

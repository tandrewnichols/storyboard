.row
  .col-xs-12
    h4.text-center What is your world called?
.row.mtl
  .col-xs-11
    form.form-horizontal(name="worldSummaryForm", novalidate, blur-track="true")
      .form-group(ng-class="{ 'has-error': worldSummaryForm.name.$blurred && !edit.name }")
        label.col-xs-2.control-label(for="name") Name
        .col-xs-8
          input#name.form-control(type="text", name="name", ng-model="edit.name", autofocus, ng-keyup="edit.slug = (edit.name | slugify)")
          div.alert.alert-danger.mtm(ng-show="!edit.name && worldSummaryForm.name.$blurred") Name cannot be blank
      .form-group
        label.col-xs-2.control-label(for="slug") Slug
        .col-xs-8
          input#slug.form-control(type="text", name="slug", ng-model="edit.slug")
        .col-xs-2(ng-include="'components/slug-info.html'", ng-controller="Slug")
      .col-xs-8.col-sm-offset-2(error="error", dismissed="dismissed", ng-show="error && !dismissed")
      .form-group
        .col-xs-8.col-sm-offset-2.btn-row
          button.btn.btn-primary(type="submit", ng-click="update()", ng-disabled="!edit.name || edit | equals : world") Update
          button.btn.btn-default(type="button", ng-click="reset()", ng-disabled="edit | equals : world") Reset
